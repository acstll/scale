{
  "name": "@scaleds/react-wrapper",
  "sideEffects": false,
  "version": "0.0.16",
  "private": true,
  "description": "React specific wrapper for @scaleds/components",
  "scripts": {
    "build": "yarn clean && yarn compile",
    "clean": "rm -rf dist",
    "compile": "yarn tsc",
    "tsc": "tsc -p ."
  },
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist/"
  ],
  "devDependencies": {
    "typescript": "^3.3.4000"
  },
  "dependencies": {},
  "peerDependencies": {
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "jest": {
    "preset": "ts-jest",
    "setupTestFrameworkScriptFile": "<rootDir>/jest.setup.js",
    "testPathIgnorePatterns": [
      "node_modules",
      "dist"
    ]
  }
}
